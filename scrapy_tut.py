#Note that Ubuntu requires special install. See tutorial in resource

# Step-1 create scrapy repo 'scrapy startproject project_name'
# Step-2 cd into the scrapy project
# Step-3 create the spider in the terminal with 'scrapy genspider mydomain mydomain.com'
# You can view list of commands with 'scrapy -h'
